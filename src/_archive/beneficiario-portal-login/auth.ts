/**
 * Nome do cookie de sessão do Portal do Beneficiário. Compartilhado entre a
 * rota de login, a de logout e a página /beneficiario (que decide se mostra
 * o formulário de login ou os itens do portal com base nesse cookie).
 */
export const BENEFICIARIO_SESSION_COOKIE = "plansul_beneficiario_session";
