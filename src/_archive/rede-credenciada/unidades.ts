/**
 * Unidades / cidades de atendimento. Preencher com dados oficiais.
 */

export type Unidade = {
  slug: string;
  cidade: string;
  estado: string;
};

export const unidades: Unidade[] = [
  // Exemplo de formato — substituir pelos dados reais:
  // { slug: "florianopolis-sc", cidade: "Florianópolis", estado: "SC" },
];
